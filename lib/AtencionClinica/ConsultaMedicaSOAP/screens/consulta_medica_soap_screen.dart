import 'package:flutter/material.dart';

class ConsultaMedicaSoapScreen extends StatelessWidget {
  const ConsultaMedicaSoapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU8 – Consulta Médica SOAP'),
      ),
    );
  }
}
