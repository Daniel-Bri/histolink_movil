import 'package:flutter/material.dart';

class FirmaDigitalDeConsultaScreen extends StatelessWidget {
  const FirmaDigitalDeConsultaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU11 – Firma Digital de Consulta'),
      ),
    );
  }
}
