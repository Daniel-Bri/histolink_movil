import 'package:flutter/material.dart';

class ConfiguracionDeConsentimientoScreen extends StatelessWidget {
  const ConfiguracionDeConsentimientoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU16 – Configuración de Consentimiento de Emergencia'),
      ),
    );
  }
}
