import 'package:flutter/material.dart';

class SolicitudDeEstudiosScreen extends StatelessWidget {
  const SolicitudDeEstudiosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU10 – Solicitud de Estudios y Carga de Resultados'),
      ),
    );
  }
}
