import 'package:flutter/material.dart';

class GestionDeIdentidadBlockchainScreen extends StatelessWidget {
  const GestionDeIdentidadBlockchainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU14 – Gestión de Identidad Blockchain'),
      ),
    );
  }
}
