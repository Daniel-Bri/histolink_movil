import 'package:flutter/material.dart';

class EdicionDeAntecedentesMedicosScreen extends StatelessWidget {
  const EdicionDeAntecedentesMedicosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU5 – Edición de Antecedentes Médicos'),
      ),
    );
  }
}
