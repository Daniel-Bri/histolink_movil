import 'package:flutter/material.dart';

class RegistroDeTriajeScreen extends StatelessWidget {
  const RegistroDeTriajeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU7 – Registro de Triaje'),
      ),
    );
  }
}
