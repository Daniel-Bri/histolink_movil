import 'package:flutter/material.dart';

class GestionDePersonalDeSaludScreen extends StatelessWidget {
  const GestionDePersonalDeSaludScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU2 – Gestión de Personal de Salud'),
      ),
    );
  }
}
