import 'package:flutter/material.dart';

class VerificacionDeIntegridadDocumentosScreen extends StatelessWidget {
  const VerificacionDeIntegridadDocumentosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU15 – Verificación de Integridad de Documentos'),
      ),
    );
  }
}
