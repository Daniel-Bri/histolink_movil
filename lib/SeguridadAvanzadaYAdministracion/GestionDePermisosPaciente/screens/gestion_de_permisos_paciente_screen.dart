import 'package:flutter/material.dart';

class GestionDePermisosPacienteScreen extends StatelessWidget {
  const GestionDePermisosPacienteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU19 – Gestión de Permisos del Paciente'),
      ),
    );
  }
}
