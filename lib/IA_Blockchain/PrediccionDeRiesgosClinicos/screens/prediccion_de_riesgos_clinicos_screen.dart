import 'package:flutter/material.dart';

class PrediccionDeRiesgosClinicosScreen extends StatelessWidget {
  const PrediccionDeRiesgosClinicosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CU13 – Predicción de Riesgos Clínicos del Paciente'),
      ),
    );
  }
}
